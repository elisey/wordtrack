version: "3.9"
services:
  wordtrack_app:
    restart: always
    build:
      context: .
    volumes:
      - ./data:/app/data
    networks:
      - nginx-network

networks:
  nginx-network:
    external: true
    name: nginx-network